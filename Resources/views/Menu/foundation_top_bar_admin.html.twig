{% extends 'IllarraCoreBundle:Menu:foundation_top_bar.html.twig' %}

{% block label %}
{% if options.allow_safe_labels and item.getExtra('safe_label', false) -%}
    {{ ('admin.menu.'~item.label)|raw|trans({}, item.getExtra('translation_domain', 'illarra_admin')) }}
{%- else -%}
    {{ ('admin.menu.'~item.label)|trans({}, item.getExtra('translation_domain', 'illarra_admin')) }}
{%- endif %}
{% endblock %}
