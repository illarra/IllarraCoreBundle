{% extends 'IllarraCoreBundle:Menu:foundation_top_bar.html.twig' %}

{% block label %}
{% if options.allow_safe_labels and item.getExtra('safe_label', false) -%}
    {{ ('admin.'~item.label)|raw|trans({}, item.getExtra('translation_domain', 'messages')) }}
{%- else -%}
    {{ ('admin.'~item.label)|trans({}, item.getExtra('translation_domain', 'messages')) }}
{%- endif %}
{% endblock %}
